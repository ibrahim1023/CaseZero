[pytest]
markers =
    live: requires CASEZERO_LIVE=1 and network access
    db: legacy local database integration test
    hosted: requires CASEZERO_HOSTED_TEST=1 and a non-production hosted Supabase project
addopts = -m "not live and not db and not hosted"
asyncio_mode = auto
