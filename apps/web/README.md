# apps/web

Investigation workspace UI (Next.js + TypeScript + Tailwind + React Flow).
The investigation state is the product; the assistant is secondary.

Deliberately empty until Phase 8 — built only after the five-case blind
pipeline gate passes (spec §46–47). Do not scaffold early.
