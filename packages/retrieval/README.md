# packages/retrieval

Hybrid retrieval over typed evidence: metadata filters + entity relationships
+ time windows + Postgres FTS + pgvector semantic similarity. Retrieval
supports the investigation; it never defines it (spec §26). pgvector embedding
dimensions ≤ 2000.

Status: planned (Phase 3, alongside the investigation engine). Embedding model
choice is open decision D2 in the foundation spec.
