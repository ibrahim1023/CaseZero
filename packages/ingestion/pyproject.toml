[project]
name = "casezero-ingestion"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["casezero-evidence"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/casezero_ingestion"]
