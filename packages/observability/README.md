# packages/observability

Model-run spans, prompt versions, cost accounting, investigation event log,
replay fold. Spans are mandatory and synchronous — a stage without spans is a
defect. See `docs/observability.md` and ADR 0008.

Status: planned (lands with the first model-calling phase).
