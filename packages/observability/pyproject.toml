[project]
name = "casezero-observability"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["casezero-evidence", "pydantic-ai-slim[openai]==2.30.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/casezero_observability"]
