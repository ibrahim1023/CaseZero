# packages/evaluation

Reveal, graders, benchmark metrics, reports. Deterministic metric math plus
calibrated LLM judges per `docs/evaluation.md`. Reads official material only
through the evaluation role after LOCK. Never mutates locked state.

Status: planned (Phase 6).
